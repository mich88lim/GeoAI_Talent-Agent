# Python dependencies for the GEO-TALENT AGENT data-ingestion script (Phase 1).
# Set up:   python -m venv .venv
# Windows:  .venv\Scripts\activate
# macOS:    source .venv/bin/activate
# Install:  pip install -r requirements.txt

pandas>=2.2
openpyxl>=3.1
python-dotenv>=1.0

# Loading into Postgres/Supabase — choose one:
supabase>=2.6          # supabase-py client
# psycopg[binary]>=3.2 # direct PostgreSQL connection (uncomment if loading via SQL instead)
