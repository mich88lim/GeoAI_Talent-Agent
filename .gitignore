# ─── Secrets — NEVER commit real keys ───────────────────────────
.env
.env.local
.env.*.local

# ─── Node / Next.js ─────────────────────────────────────────────
node_modules/
.next/
out/
build/
dist/
*.log
npm-debug.log*
pnpm-debug.log*
.pnpm-store/
*.tsbuildinfo
next-env.d.ts

# ─── Python (ingestion + scripts) ───────────────────────────────
.venv/
venv/
__pycache__/
*.pyc
.python-version

# ─── PII DATA — DO NOT COMMIT teacher names/emails ──────────────
# Load the cleaned workbook into the database via Phase 1 ingestion.
/db/seed/*.xlsx
/db/seed/*.xls
/db/seed/*.csv
/data/

# ─── Tests / coverage ───────────────────────────────────────────
coverage/
.nyc_output/

# ─── OS / editor ────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.pem

# ─── Vercel ─────────────────────────────────────────────────────
.vercel

# ─── Yarn ───────────────────────────────────────────────────────
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

mkdir -p db/seed
# copy MASTER_LIST_V2_GeoAI_CLEANED.xlsx into db/seed/
# confirm git doesn't see it:
git status    # should NOT show the .xlsx